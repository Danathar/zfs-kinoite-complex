__pycache__/
*.pyc
.pytest_cache/
artifacts/
docs/private/
cosign.key
.DS_Store
.codex
# Everything under .claude/ is per-machine session state, except the two
# directories below, which are shared and reviewed like any other file.
.claude/*
!.claude/commands/
!.claude/memory/
.coverage
coverage.json
